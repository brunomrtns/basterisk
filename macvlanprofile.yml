description: Untagged macvlan INCUS profile
devices:
  eth0:
    name: eth0
    nictype: macvlan
    parent: enp2s0
    type: nic
  root:
    path: /
    pool: default
    type: disk
config:
  limits.cpu: "4"
  limits.memory: "4GiB"
  security.privileged: "true"
  boot.autostart: "false"
